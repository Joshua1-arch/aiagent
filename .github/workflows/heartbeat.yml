name: Agent Heartbeat Keeper
on:
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: windows-latest
    steps:
      - name: Info
        run: Write-Host "Manual heartbeat disabled — session is TEE-bound. Run 'onchainos agent heartbeat --chain-index 196' locally instead."
        shell: pwsh
